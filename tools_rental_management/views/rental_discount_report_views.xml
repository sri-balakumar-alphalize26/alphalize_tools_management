<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Remove the old act_window record that conflicts -->
    <delete model="ir.actions.act_window" search="[('xml_id', '=', 'tools_rental_management.action_rental_discount_report')]"/>

    <record id="action_rental_discount_report_dashboard" model="ir.actions.client">
        <field name="name">Discount Details</field>
        <field name="tag">rental_discount_report_dashboard</field>
    </record>
</odoo>
