<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Dashboard Action (opens Rental Orders with pivot) -->
    <record id="action_rental_dashboard" model="ir.actions.act_window">
        <field name="name">Rental Dashboard</field>
        <field name="res_model">rental.order</field>
        <field name="view_mode">graph,pivot,list,calendar</field>
        <field name="context">{}</field>
    </record>
</odoo>
